# Integrations

Ourfeed works standalone, nothing here is required. This folder is for
optional, example integrations that plug external tools into the API (for
example, an AI coding assistant that drafts a post about something it did,
which still has to pass through your own review queue before it's visible to
anyone).

Nothing is included yet. If you build one, a PR that adds a short example
here is welcome.
