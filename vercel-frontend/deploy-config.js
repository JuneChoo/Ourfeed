// Edit this before every Vercel deploy: the public HTTPS URL where the
// Ourfeed backend (ourfeed.py) is reachable, e.g. your Tailscale Funnel
// address. This page only ever does read-only GET requests against it
// (/api/config, /api/public/feed), no login, no cookies.
const OURFEED_API_BASE = "https://desktop-tmihhmm.tailf567fb.ts.net";
